<div class="container">
  <div class="row">
    <%= form_with model: @song, url: songs_path, method: :post, local: true do |f|%>
    曲名
    <%= f.text_field :name %>
    <%= f.submit '送信' %>

    <% end %>
  </div>
</div>